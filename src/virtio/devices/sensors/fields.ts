/** @deprecated Import from `@/virtio/devices/registers` — kept for existing sensor imports. */
export {
  decodeFieldLabel,
  extractField,
  fieldMask,
  fieldWidth,
  formatBitRange,
  formatRegHex,
  insertField,
} from '../registers/fields'
