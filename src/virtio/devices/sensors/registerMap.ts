/** @deprecated Import from `@/virtio/devices/registers` — kept for existing sensor imports. */
export {
  mergeRegisterMap,
  parseHexish,
  registersFromJson,
  type FieldMapJson,
  type RegisterMapEntryJson,
  type RegisterMapJson,
} from '../registers'
