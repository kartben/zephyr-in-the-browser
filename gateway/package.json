{
  "name": "zephyr-in-the-browser-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket → passt network gateway for zephyr-in-the-browser guests",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test server.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
