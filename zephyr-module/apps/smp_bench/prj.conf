CONFIG_PRINTK=y
CONFIG_THREAD_NAME=y

# k_thread_cpu_pin(). Without it the scheduler still spreads the workers over
# the idle CPUs, but which thread lands where changes run to run, and the
# "CPUs used" column is what makes the speedup believable.
CONFIG_SCHED_CPU_MASK=y
