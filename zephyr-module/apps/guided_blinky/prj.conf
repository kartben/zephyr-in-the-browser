# Stock blinky's requirement, plus the annotations that explain it.
CONFIG_GPIO=y

# The lesson. Off by default everywhere else, so an annotated sample and an
# unannotated one produce the same binary unless it is asked for.
CONFIG_SAMPLE_ANNOTATIONS=y
