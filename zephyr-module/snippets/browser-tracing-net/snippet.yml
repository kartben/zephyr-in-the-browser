name: browser-tracing-net
description: >
  Net-object CTF width fix for traced networking samples. Apply *after*
  browser-tracing (and after conf/net.conf) so NET_IPV6 can override the
  IPv4-sandbox default — see conf/tracing-net.conf.

append:
  EXTRA_CONF_FILE: browser-tracing-net.conf
