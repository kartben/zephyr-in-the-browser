# Stock samples/drivers/buzzer/tone against a gpio-buzzer on the browser GPIO.
#
# Frequency arguments are on/off only for the GPIO backend (non-zero = sound).

CONFIG_GPIO=y
CONFIG_BUZZER=y

CONFIG_MAIN_STACK_SIZE=4096
