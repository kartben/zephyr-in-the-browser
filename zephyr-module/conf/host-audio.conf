# I2S subsystem, so the qemu,host-audio driver (and its `hostaudio` shell
# commands) build in. The bridge is reachable from the Zephyr shell without
# writing an application: `hostaudio beep` queues a tone the browser plays.
CONFIG_I2S=y
