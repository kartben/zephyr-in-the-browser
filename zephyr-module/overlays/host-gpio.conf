# GPIO subsystem plus the `gpio` shell command, so the host-backed pins are
# reachable from the Zephyr shell without writing an application: `gpio get`
# reads a button the browser raised, `gpio set` drives an LED it displays.
CONFIG_GPIO=y
CONFIG_GPIO_SHELL=y
