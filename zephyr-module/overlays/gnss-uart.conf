# Zephyr's generic NMEA driver uses the modem UART backend, which requires
# either interrupt-driven or asynchronous UART reception. Both QEMU PL011
# devices support the interrupt-driven API.
CONFIG_UART_INTERRUPT_DRIVEN=y
