# Sensor subsystem plus the `sensor` shell command, so the host-fed readings
# are reachable from the Zephyr shell without writing an application.
CONFIG_SENSOR=y
CONFIG_SENSOR_SHELL=y

# The stock shell_module sample turns the boot banner off.
CONFIG_BOOT_BANNER=y
